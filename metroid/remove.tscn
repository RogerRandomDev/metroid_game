[gd_scene load_steps=3 format=2]

[ext_resource path="res://textures/People-no-bg.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Sprite


# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"


# Called when the node enters the scene tree for the first time.
func _ready():
	var starting = get_texture().get_data()
	starting.lock()
	var size=Vector2(get_texture().get_width(),get_texture().get_height())
	for x in size.x:for y in size.y:
		if starting.get_pixel(x,y).a!=0:
			starting.set_pixel(x,y,Color(1,1,1,1))
	starting.save_png(\"res://textures/entities.png\")
"

[node name="Sprite" type="Sprite"]
texture = ExtResource( 1 )
script = SubResource( 1 )
